#!/usr/bin/python3
''' A module to check if the object is instance or not '''


def is_same_class(obj, a_class):
    ''' A function to return True or Flase for the instance '''

    return isinstance(obj, a_class)
